# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY. Edit /Dockerfile.erb and run /update instead.

FROM centos:7

RUN set -ex \
          && yum -y install \
      ca-certificates \
      curl \
      gcc \
      glibc-devel \
      make \
              readline-devel \
                        && curl -fsSL -o /tmp/lua.tar.gz "https://www.lua.org/ftp/lua-5.2.4.tar.gz" \
  && cd /tmp \
  
    && echo "ef15259421197e3d85b7d6e4871b8c26fd82c1cf *lua.tar.gz" | sha1sum -c - \
    && mkdir /tmp/lua \
  && tar -xf /tmp/lua.tar.gz -C /tmp/lua --strip-components=1 \
  && cd /tmp/lua \
      && make linux \
    && make install \
      && cd / \
      && yum -y history undo last \
          && yum -y install readline \
                  && yum clean all \
    && rm -rf /var/cache/yum \
    && rm -rf /tmp/lua /tmp/lua.tar.gz \
    && lua -v

CMD ["lua"]
