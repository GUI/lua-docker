# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY. Edit /Dockerfile.erb and run /update instead.

FROM alpine:3.9

RUN set -ex \
      && apk add --no-cache --virtual .lua-builddeps \
      ca-certificates \
      curl \
      gcc \
      libc-dev \
      make \
              readline-dev \
                        && curl -fsSL -o /tmp/lua.tar.gz "https://www.lua.org/work/lua-5.4.0-beta-rc2.tar.gz" \
  && cd /tmp \
  && echo "961e2692a10a4a3c6fe80086e4cbefd5 *lua.tar.gz" | md5sum -c - \
  && mkdir /tmp/lua \
  && tar -xf /tmp/lua.tar.gz -C /tmp/lua --strip-components=1 \
  && cd /tmp/lua \
      && make linux \
    && make install \
      && cd / \
      && apk add --no-network --no-cache --virtual .lua-rundeps \
              readline \
                              && apk del --no-network .lua-builddeps \
    && rm -rf /tmp/lua /tmp/lua.tar.gz \
    && lua -v

CMD ["lua"]
