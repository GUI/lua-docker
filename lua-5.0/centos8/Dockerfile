# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY. Edit /Dockerfile.erb and run /update instead.

FROM centos:8

RUN set -ex \
            && yum -y upgrade \
        ca-certificates \
        curl \
    
    && yum -y install \
      ca-certificates \
      curl \
      gcc \
      glibc-devel \
      make \
              readline-devel \
                        && curl -fsSL -o /tmp/lua.tar.gz "https://www.lua.org/ftp/lua-5.0.3.tar.gz" \
  && cd /tmp \
  && echo "feee27132056de2949ce499b0ef4c480 *lua.tar.gz" | md5sum -c - \
  && mkdir /tmp/lua \
  && tar -xf /tmp/lua.tar.gz -C /tmp/lua --strip-components=1 \
  && cd /tmp/lua \
      && make \
    && make install \
      && cd / \
      && yum -y history undo last \
          && yum -y install readline \
                  && yum clean all \
    && rm -rf /var/cache/yum \
    && rm -rf /tmp/lua /tmp/lua.tar.gz \
    && lua -v

CMD ["lua"]
