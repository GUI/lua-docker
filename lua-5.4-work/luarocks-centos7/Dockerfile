# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY. Edit /Dockerfile.erb and run /update instead.

FROM centos:7

RUN set -ex \
      && yum -y install \
      ca-certificates \
      curl \
      gcc \
      glibc-devel \
      make \
              readline-devel \
                            dirmngr \
        gnupg2 \
        unzip \
          && curl -fsSL -o /tmp/lua.tar.gz "https://www.lua.org/work/lua-5.4.0-work2.tar.gz" \
  && cd /tmp \
  && echo "3cdf2a4eb84dde6b6aaf5d2d1de07be9 *lua.tar.gz" | md5sum -c - \
  && mkdir /tmp/lua \
  && tar -xf /tmp/lua.tar.gz -C /tmp/lua --strip-components=1 \
  && cd /tmp/lua \
      && make linux \
    && make install \
        && curl -fsSL -o /tmp/luarocks.tar.gz "https://luarocks.org/releases/luarocks-3.1.2.tar.gz" \
    && curl -fsSL -o /tmp/luarocks.tar.gz.asc "https://luarocks.org/releases/luarocks-3.1.2.tar.gz.asc" \
    && cd /tmp \
    && export GNUPGHOME="$(mktemp -d)" \
    && export GPG_KEYS="8460980B2B79786DE0C7FCC83FD8F43C2BB3C478" \
    && (gpg --batch --keyserver hkp://ha.pool.sks-keyservers.net:80 --recv-keys "$GPG_KEYS" || gpg --batch --keyserver hkp://ipv4.pool.sks-keyservers.net:80 --recv-keys "$GPG_KEYS") \
    && gpg --batch --verify luarocks.tar.gz.asc luarocks.tar.gz \
    && rm -rf "$GNUPGHOME" \
    && mkdir /tmp/luarocks \
    && tar -xf /tmp/luarocks.tar.gz -C /tmp/luarocks --strip-components=1 \
    && cd /tmp/luarocks \
    && ./configure \
    && make \
    && make install \
    && cd / \
      && yum -y history undo last \
          && yum -y install readline \
                    && yum -y install \
        ca-certificates \
        curl \
        unzip \
        && yum clean all \
    && rm -rf /var/cache/yum \
    && rm -rf /tmp/lua /tmp/lua.tar.gz \
      && rm -rf /tmp/luarocks /tmp/luarocks.tar.gz \
    && luarocks --version \
    && lua -v

CMD ["lua"]
